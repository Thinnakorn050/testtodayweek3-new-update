name: connection_demo
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.3.0

dependencies:
  http: ^1.1.1   # อัปเดตเป็นเวอร์ชันล่าสุด
  mysql1: ^0.20.0
  # path: ^1.8.0  # หากจำเป็นต้องใช้ก็เพิ่มเข้ามา

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
